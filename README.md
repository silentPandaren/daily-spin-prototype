# Daily Spin — Прототип колеса фортуны

Интерактивное колесо фортуны для MY.GAMES Market с системой призов, Pity Timer и аналитикой.

## 📋 Описание

Daily Spin — это клиентское веб-приложение "Колесо фортуны", где пользователи тратят "Spin Coupons" (купоны на спин) для получения призов. Прототип реализует полную игровую механику с взвешенным распределением вероятностей, системой гарантированных призов и отслеживанием событий.

### Основные возможности

- 🎰 **8-секторное колесо** с визуально привлекательной анимацией
- 🎁 **8 различных призов** с настраиваемыми шансами выпадения
- ⏱️ **Pity Timer** — гарантированный легендарный приз после 10 спинов
- 📊 **Система аналитики** — отслеживание всех событий в реальном времени
- 💰 **Upsell-предложение** — специальное предложение при исчерпании купонов
- 📱 **Адаптивный дизайн** — корректная работа на всех устройствах
- 🎨 **Дизайн в стиле MY.GAMES** — темная тема с фиолетово-синими градиентами

## 🚀 Быстрый старт

### Запуск локально

1. Клонируйте репозиторий:
```bash
git clone https://github.com/silentPandaren/daily-spin-prototype.git
cd daily-spin-prototype
```

2. Откройте `index.html` в браузере:
   - Просто дважды кликните на файл `index.html`
   - Или используйте локальный сервер (рекомендуется):
     ```bash
     # Python 3
     python -m http.server 8000
     
     # Node.js (http-server)
     npx http-server
     ```

3. Откройте в браузере: `http://localhost:8000`

### Требования

- Современный браузер с поддержкой:
  - HTML5 Canvas API
  - CSS3 (переменные, градиенты, анимации)
  - ES6+ JavaScript
- Рекомендуемые браузеры:
  - Chrome 90+
  - Firefox 88+
  - Safari 14+
  - Edge 90+

## 📁 Структура проекта

```
daily-spin-prototype/
├── index.html              # Основной HTML файл
├── styles.css              # Все CSS стили
├── script.js               # Вся JavaScript логика
├── icons/                  # Иконки призов
│   ├── discount.png
│   ├── platinum.png
│   ├── legendary.png
│   ├── spheres.png
│   ├── cores.png
│   └── premium.png
├── rush-royale-logo.png   # Логотип игры
├── rush-royale-bg.png     # Фоновое изображение для баннера
├── README.md              # Этот файл
├── TECHNICAL_SPECIFICATION.md      # Техническая спецификация (EN)
└── TECHNICAL_SPECIFICATION_RU.md   # Техническая спецификация (RU)
```

## 🎮 Как использовать

### Для пользователя

1. **Начало работы**: При загрузке страницы у вас есть 3 купона на спин
2. **Крутка колеса**: Нажмите кнопку "🎰 Spin!" для запуска
3. **Получение приза**: После остановки колеса появится модальное окно с выигранным призом
4. **Получение купонов**: Купоны можно получить через покупки в MY.GAMES Market или ежедневный вход

### Для разработчика

#### Настройка призов

Откройте `script.js` и найдите массив `prizesConfig`:

```javascript
const prizesConfig = [
  { name: '25% Discount', wheelText: '25%\nDiscount', color: '#005aff', icon: 'icons/discount.png', weight: 5 },
  { name: '100 Platinum', wheelText: '100\nPlatinum', color: '#642ab5', icon: 'icons/platinum.png', weight: 15 },
  // ... другие призы
];
```

**Параметры:**
- `name` — название приза (отображается в модальном окне)
- `wheelText` — текст на колесе (поддерживает `\n` для переноса строки)
- `color` — цвет сектора (hex-код)
- `icon` — путь к иконке
- `weight` — вес вероятности (должен суммироваться до 100)

#### Настройка Pity Timer

В `script.js` найдите константы:

```javascript
const LEGENDARY_IDX = 2;        // Индекс легендарного приза
const PITY_THRESHOLD = 10;     // Количество спинов до гарантии
```

#### Тестовая кнопка

Для тестирования используйте кнопку "+ 10 coupons (test)" в правой колонке.

## 🎯 Призы и шансы

| Приз | Шанс | Вес |
|------|------|-----|
| 25% Discount | 5% | 5 |
| 100 Platinum | 15% | 15 |
| Legendary Item | 5% | 5 |
| 8 Spheres | 5% | 5 |
| 200 Cores | 5% | 5 |
| 4 Spheres | 10% | 10 |
| 50 Platinum | 30% | 30 |
| Premium 30 days | 25% | 25 |

**Pity Timer**: После 10 спинов без легендарного приза следующий спин гарантированно даст "Legendary Item".

## 🔧 Технические детали

### Архитектура

- **HTML**: Структура страницы, семантическая разметка
- **CSS**: Модульные стили с CSS-переменными, адаптивный дизайн
- **JavaScript**: Модульная структура с IIFE, разделение логики на функции

### Основные функции

#### `drawWheel(angle)`
Отрисовывает колесо на canvas с учетом текущего угла поворота.

#### `startSpin()`
Инициирует спин: определяет приз, запускает анимацию, обновляет счетчики.

#### `rollPrize()`
Генерирует случайное число и определяет выигранный приз на основе весов.

#### `trackEvent(eventName, data)`
Централизованная функция для отслеживания событий аналитики.

#### `updatePityUI()`
Обновляет визуальное отображение прогресса Pity Timer.

### События аналитики

- `page_loaded` — загрузка страницы
- `spin_started` — начало спина
- `prize_received` — получение приза
- `spin_attempt_no_coupons` — попытка спина без купонов
- `Shop_Redirect_from_Wheel` — переход в магазин из upsell-предложения

## 📊 Debug Panel

Внизу страницы находится панель отладки, которая показывает:

- Сгенерированное случайное число
- Выбранный диапазон и процент
- Результат спина
- Текущий счетчик Pity Timer
- Общее количество спинов
- Среднее количество спинов в минуту
- Лог всех событий аналитики

## 🎨 Кастомизация

### Изменение цветов

Откройте `styles.css` и найдите секцию `:root` с CSS-переменными:

```css
:root {
  --colorPrimary: #005aff;
  --colorBgLayout: #0a0a0a;
  --colorText: #ffffff;
  /* ... другие переменные */
}
```

### Изменение шрифтов

В `styles.css` измените `@import` для Google Fonts или замените `font-family` в `body`.

### Добавление новых призов

1. Добавьте иконку в папку `icons/`
2. Добавьте запись в `prizesConfig` в `script.js`
3. Убедитесь, что сумма всех `weight` равна 100

## 🔒 Безопасность

⚠️ **Важно**: Текущий прототип выполняет всю логику на клиенте. Для продакшена необходимо:

- Перенести определение призов на сервер
- Хранить баланс купонов в базе данных
- Сохранять счетчик Pity Timer на сервере
- Валидировать все запросы на бэкенде

Подробнее см. `TECHNICAL_SPECIFICATION_RU.md`.

## 📚 Документация

- **[TECHNICAL_SPECIFICATION_RU.md](TECHNICAL_SPECIFICATION_RU.md)** — Полная техническая спецификация на русском
- **[TECHNICAL_SPECIFICATION.md](TECHNICAL_SPECIFICATION.md)** — Technical specification in English

## 🐛 Известные ограничения

- Все данные хранятся в памяти (сбрасываются при обновлении страницы)
- Логика определения призов выполняется на клиенте (уязвимо для манипуляций)
- Нет интеграции с реальным бэкендом
- Нет системы аутентификации

## 🚧 Планы на будущее

- [ ] Интеграция с бэкенд API
- [ ] Персистентное хранение состояния
- [ ] Система аутентификации
- [ ] История спинов
- [ ] Статистика выигрышей
- [ ] Мультиязычность

## 📝 Лицензия

Этот проект является прототипом для внутреннего использования MY.GAMES.

## 👥 Авторы

Разработано для MY.GAMES Market.

## 🤝 Вклад

Для предложений и исправлений создавайте Issues или Pull Requests в репозитории.

---

**Версия**: 1.0  
**Последнее обновление**: 24 февраля 2026

